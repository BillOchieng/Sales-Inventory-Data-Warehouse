# If you're using a virtual environment (like venv/), always include that in .gitignore.

# Don't track *.csv or *.xlsx unless it's demo/sample data.

# If you use st.file_uploader(), make sure uploaded files aren't saved permanently in your project unless needed.

# Python cache and bytecode
__pycache__/
*.py[cod]
*.pyo
*.pyd

# Virtual environments (common names)
venv/
env/
.venv/
newenv/

# Jupyter/IPython notebooks checkpoints
.ipynb_checkpoints/

# IDE settings
.vscode/
.idea/

# macOS system files
.DS_Store

# Logs
*.log

# Uploaded files or large datasets (uncomment if needed)
# *.csv
# *.xlsx
# *.mp3
# *.mp4

# Streamlit secrets (optional: only ignore if containing sensitive data)
.streamlit/credentials.toml

# If you have secrets or config files, add them here (but don't ignore config you want to share!)
# .env
